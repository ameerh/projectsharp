<%= form_for(@espn_game) do |f| %>
  <% if @espn_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@espn_game.errors.count, "error") %> prohibited this espn_game from being saved:</h2>

      <ul>
      <% @espn_game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
